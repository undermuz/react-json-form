import { ArrowDownLine, ArrowUpLine, Plus, Trash } from "@rsuite/icons"
import type { JsonFormIcons } from "@undermuz/react-json-form"

const Icons: JsonFormIcons = {
    Tabs: {
        Remove: Trash,
        Add: Plus,
        MoveUp: ArrowUpLine,
        MoveDown: ArrowDownLine,
    },
}

export default Icons
